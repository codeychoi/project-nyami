<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.ReviewMapper">

	<select id="getAllReviews" resultType="ReviewDomain">
        SELECT *
        FROM REVIEW
        ORDER BY created_at DESC
    </select>
    
<!-- 리뷰 삽입 쿼리 -->
    <insert id="insertReview" parameterType="ReviewDomain">
        INSERT INTO review (user_id, store_id, score, review, created_at)
        VALUES (#{userId}, #{storeId}, #{score}, #{review}, #{createdAt})
    </insert>
	
</mapper>