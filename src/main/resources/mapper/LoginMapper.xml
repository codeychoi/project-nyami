<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.project.mapper.LoginMapper">

    <!-- 사용자 조회 쿼리 -->
    <select id="getUser" parameterType="String" resultType="com.project.domain.LoginDomain">
        SELECT * FROM User_data WHERE userid = #{userid}
    </select>

    <!-- 회원가입 쿼리 -->
    <insert id="insertUser" parameterType="com.project.domain.LoginDomain">
        INSERT INTO User_data (id, userid, nickname, userpwd, email, join_date)
        VALUES (user_data_seq.NEXTVAL, #{userid}, #{nickname}, #{userpwd}, #{email}, CURRENT_TIMESTAMP)
    </insert>

</mapper>